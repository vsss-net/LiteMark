# LiteMark 环境变量配置示例

# ============================================
# 前端配置
# ============================================

# 前端调用 API 的基础地址
# 如果前端和 API 部署在同一域名下，可以留空
# 如果 API 部署在不同域名，需要填写完整的 API 地址（如 https://api.example.com）
#VITE_API_BASE_URL=






# ============================================
# 后端配置
# ============================================

# JWT 令牌签名密钥
JWT_SECRET=your-secret-key-change-in-production

# CORS 跨域允许的来源
# 设置为 * 允许所有来源（开发环境）
# 生产环境建议设置为具体的域名（如 https://example.com）
CORS_ORIGIN=*


# Vercel Postgres 数据库配置
# 以下变量在 Vercel 上部署时，连接 Verce Neon Serverless Postgres 数据库后会自动提供，无需手动配置
# POSTGRES_URL=postgres://user:password@host:port/database
# POSTGRES_PRISMA_URL=postgres://user:password@host:port/database?pgbouncer=true
# POSTGRES_URL_NON_POOLING=postgres://user:password@host:port/database


Cron Job 安全密钥（用于保护自动备份端点）
# CRON_SECRET=""