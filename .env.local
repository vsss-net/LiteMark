
JWT_SECRET="replace-with-your-secret"           # JWT 签名密钥，务必使用随机字符串
ADMIN_USERNAME="admin"                         # 后台登录用户名
ADMIN_PASSWORD="admin123"                      # 后台登录密码
CORS_ORIGIN="*"                                # 允许的跨域来源，示例：https://example.com

VITE_API_BASE_URL=""                            # 自定义 API 基址，留空表示同源


BOOKMARKS_KEY="litemark/data/bookmarks.json"       # 书签存储路径（可选）
SETTINGS_KEY="litemark/data/settings.json"         # 主题存储路径（可选）

STORAGE_DRIVER="vercel-blob"            # 存储驱动切换：vercel-blob（默认）、oss、webdav

# Vercel Blob（默认）
VERCEL_BLOB_READ_WRITE_TOKEN=""                # Vercel Blob Read/Write Token


# OSS / 兼容对象存储（Cloudflare R2、腾讯云COS、阿里云OSS 等）
OSS_REGION=""                                   # 区域，例如 us-east-1
OSS_BUCKET=""                                   # 桶名称
OSS_ACCESS_KEY_ID=""                            # 访问密钥 ID
OSS_SECRET_ACCESS_KEY=""                        # 访问密钥 Secret
OSS_ENDPOINT=""                                 # 自定义 Endpoint（非必填）
OSS_FORCE_PATH_STYLE="false"                    # 若需 Path-Style 访问设为 true

# WebDAV（Nextcloud、坚果云等）
WEBDAV_URL="https://example.com/dav/path/"     # WebDAV Root URL
WEBDAV_USERNAME=""                             # WebDAV 用户名（如需验证）
WEBDAV_PASSWORD=""                             # WebDAV 密码